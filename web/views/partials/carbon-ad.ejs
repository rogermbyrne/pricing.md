  <!--
    Carbon Cover ad unit (BuySellAds). One per page — carbon.js keys off the
    #_carbonads_js id and injects into this script tag's parent, so the container
    reserves 280px to keep the injection from shifting layout.
    Styling hook: #carbon-cover. Hosts are allowlisted in the CSP in web/server.ts.
  -->
  <div class="my-12 flex justify-center">
    <div class="w-full max-w-[340px]" style="min-height: 280px;">
      <script async type="text/javascript" src="//cdn.carbonads.com/carbon.js?serve=CWBDV5Q7&placement=latestsh&format=cover" id="_carbonads_js"></script>
    </div>
  </div>
